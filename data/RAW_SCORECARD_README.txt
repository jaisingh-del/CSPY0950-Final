The raw College Scorecard CSV is not bundled here -- it's ~100MB.

To re-run `data_cleaner.py`, download "Most Recent Cohorts: Institution"
from https://collegescorecard.ed.gov/data and save it in this folder as
`raw_scorecard.csv`. The game itself doesn't need the raw file --
`cleaned_schools.csv` is already committed.
